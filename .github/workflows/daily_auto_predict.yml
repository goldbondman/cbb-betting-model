name: Daily Auto Predict

on:
  schedule:
    - cron: "0 15 * * *"
  workflow_dispatch: {}

jobs:
  auto-predict:
    runs-on: ubuntu-latest
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      MODEL_VERSION: ${{ secrets.MODEL_VERSION }}
      EDGE_MIN: "3.0"
      EDGE_TIER2: "6.0"
      EDGE_TIER3: "9.0"
      DAYS_AHEAD: "1"
      DAYS_BACK: "3"
      RAW_SCHEMA: "raw"

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          # Recommend matching run-ml-pipeline.yml to avoid version drift
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install psycopg[binary]

      - name: Run ESPN builder (3-day backfill)
        run: |
          python espn_boxscore_builder.py
        env:
          DAYS_BACK: ${{ env.DAYS_BACK }}

      - name: Verify feature store exists
        run: |
          ls -lh *.csv *.json || true
          test -f espn_team_game_features.csv

      - name: Train models (creates ml/models/*.json)
        run: |
          python ml/train_ml_models.py

      - name: Verify model artifacts exist
        run: |
          ls -lh ml/models || true
          test -f ml/models/margin_model.json

      - name: Run daily auto prediction
        run: |
          python scripts/daily_auto_predict.py
